{
  "name": "ci-triage-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Fetches failed GitHub Actions logs, summarizes the root cause with Claude, and posts it as a PR comment.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "@octokit/rest": "^21.1.1",
    "unzipper": "^0.12.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/unzipper": "^0.10.11",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
